## 1. Sidecar: scene-cut detection

- [x] 1.1 Implement histogram-based cut detection in `detector.py`: sample frames every ~2s across the requested time range, compute per-frame color histograms, flag a cut where consecutive histogram distance exceeds a threshold. (`_frame_histogram`/`_detect_cuts`, HSV hue/sat histogram + `HISTCMP_CORREL`)
- [x] 1.2 Cap the number of detected segments per clip (e.g. 4) - keep only the strongest histogram jumps if more are found. (`MAX_SEGMENTS = 4`, keeps top `MAX_SEGMENTS-1` cuts by distance)
- [x] 1.3 Implement `find_crop_segments()`: run cut detection first, then call the existing per-clip face-detection logic once per resulting segment range, returning a list of `{startTime, endTime, xOffset, faceCount, confidence}` (or `found:false` per segment with no usable face).
- [x] 1.4 Update `/crop-position` in `server.py` to return the segment list (extend the existing response shape - top-level `found`/`xOffset`/`faceCount`/`confidence` mirror the most-confident segment, so a caller reading only those fields still works; `segments` is the new array).
- [x] 1.5 Unit-test cut detection against the real footage that exposed the bug. **Caveat found and accepted:** could only test against the already-cropped 1080-wide output clip (not the 7.5GB landscape original - same trade-off as the parent change's 1.5), so `xOffset` always clamps to 0 in this test (no room to move in an already-1080-wide frame) - only the SEGMENTATION TIMING was validated this way, not the numeric offset. Real numeric-offset validation happens in task 4 against a fresh production run. Result: detected cuts at 18.0/26.0/56.0/78.0/84.0/106.0s (6 candidates over threshold), capped to the 3 strongest → segments `[0-55, 55-77, 77-83, 83-158]`. The 55s and 106s(~ not kept, see below) boundaries line up closely with the wide→Geraldo-listening transition (~48-57s) and Geraldo-listening→Geraldo-talking transition (~102-111s) found by manual frame review - the two transitions that actually mattered for the reported bug. The weaker mid-section cuts (78s/84s, both still within the "wide shot / Renan speaking" span) got picked over the ~106s one since only 3 slots exist; accepted since that span's segments still each find a real face with high confidence (0.97-0.976).

## 2. Deployment

- [x] 2.1 Rebuild and redeploy the sidecar container with the updated code. (`docker compose -f deploy/docker-compose.yml up -d --build` on the VPS)
- [x] 2.2 Confirm `/health` still responds and `/crop-position` still returns a well-formed response for a known-good single-segment case. Confirmed `/health` OK; `find_crop_segments` against the same podcast-01/03 fixtures used in the parent change returns a single segment (no cut detected in those, as expected - they're single continuous takes) with the same face-count/confidence values as `find_crop_position` returned before this change, i.e. no regression.

## 3. n8n workflow integration (`Zfwhv4pppJf3NhkR`)

- [x] 3.1 Update "FFmpeg Cortar 9:16 Podcast": replace the single-crop ffmpeg call with a loop over returned segments - one `ffmpeg -ss/-to ... -vf crop=...` sub-cut per segment (each writing to its own temp file), then a `ffmpeg -f concat` pass joining them into the final `outPath`. Fallback (no segments / sidecar error) still produces one cut with the fixed center-crop formula, same as before. `SEGMENTS` string built entirely by the n8n expression evaluator (JS `.map().join('|')` on the sidecar's response) so bash never needs to parse JSON (no `jq` on the hardened image).
- [x] 3.2 Validate the updated bash locally (`sh -n`) against representative substituted values for 1-segment and multi-segment cases. **Went further than static validation** - ran the actual segment-cut-loop logic for real against a synthetic test video inside the n8n container (has ffmpeg) and found 2 real runtime bugs static syntax checking couldn't catch: (1) `awk 'BEGIN{...}'` with no filename argument was implicitly reading from the SAME stdin pipe feeding the `while read` loop, corrupting the 2nd+ segment's parsed values (busybox awk quirk - fixed with `</dev/null` on every such `awk` call); (2) each `ffmpeg` call inside the loop was also contending for that same piped stdin (interactive-control input), which crashed the 2nd ffmpeg invocation with `Undefined constant or missing '(' in ''` - fixed by adding `-nostdin` to every ffmpeg call inside the loop (and the concat pass). Re-validated end-to-end with the actual generated command text (3 segments + concat) against a synthetic video - produced a correct 1080x1920, 20s output.
- [x] 3.3 Apply via MCP (`update_workflow`) and `publish_workflow`, confirming byte-for-byte that Shorts/Palavra Completa and the rest of the podcast workflow remain untouched. Only 1 operation applied (`updateNodeParameters` on "FFmpeg Cortar 9:16 Podcast" - no nodes added/removed, 40 nodes before and after). Re-fetched and confirmed the command matches the intended string byte-for-byte. Published - `activeVersionId: 62bc4fac-ff2e-4628-9389-4c917fe771ee`.

## 4. Validation

- [x] 4.1 Re-run the same test episode (or the specific problem clip) through the updated pipeline. Execution `1692` completed successfully (`status:"success"`), ran 3h16min (19:50:48-23:07:38 UTC). Produced 7 clips - the 2nd-pass AI picked different highlight windows than the prior run (expected nondeterminism), so no clip landed on the exact same timestamps as `o-manicomio-do-iva` - but that's fine, the fix is generic and this run exercised it on whatever multi-camera-angle clips the AI happened to pick this time.
- [x] 4.2 Extract frames and confirm the crop follows camera cuts. **Real multi-segment behavior confirmed in production data:** of 7 clips, 2 got real multi-segment crops with different `xOffset` per segment (`pix-nao-e-imposto`: 3 segments, offsets 748/313/896; `quem-se-beneficia-da-reforma`: 2 segments, offsets 544/1156), 3 got a single segment (continuous takes, as expected), and **2 failed with a sidecar HTTP timeout** (`timeout of 15000ms exceeded` - the longer clips, 143s and 176s, now sample more densely for cut detection than the parent change's single-clip case and can exceed 15s) - correctly fell back to the fixed center crop per the "pipeline never blocks" requirement, no clip lost or corrupted. **Fix applied:** raised the "Detectar Rosto Ativo Podcast" node's timeout from 15000ms to 30000ms. Extracted frames from the 2 real multi-segment clips at points inside each segment and visually confirmed the crop changes correctly with the camera angle (close-up → wide 3-shot → close-up, each well-framed) - screenshots reviewed directly.
  - **Second real bug found and fixed from this same production data:** segment boundaries are computed by the sidecar against the RAW `clipStart`/`clipEnd` sent to it, but the actual cut range is `ASTART`/`AEND` (post-silence-snap), which can extend beyond that raw range - the clamp logic only ever *shrank* segments to fit `[ASTART,AEND]`, never *extended* them, so the silence-snap extension was silently dropped. Confirmed with real data: `pix-nao-e-imposto`'s `real_start`/`real_end` promised 53.59s, the actual output file was 47.95s (`ffprobe`) - missing ~5.6s. Fixed by forcing the first kept segment's start to `ASTART` and the last kept segment's end to `AEND` (via an `awk` pass over `$SEGMENTS` before the cut loop) instead of only clamping inward. Validated with a real end-to-end run against a synthetic video (3 segments) - output duration now matches `AEND-ASTART` exactly. Applied and published to production alongside the timeout fix.
- [x] 4.3 Spot-check previously-correct single-take clips for regression. 3 of the 7 clips from execution `1692` got a single segment (no cut detected), matching the expected no-regression behavior for continuous-take clips.
- [x] 4.4 Document the outcome in CLAUDE.md.
