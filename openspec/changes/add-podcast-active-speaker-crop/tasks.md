## 1. Sidecar service

- [x] 1.1 Scaffold the sidecar: Python + `opencv-python-headless` + a minimal HTTP framework (FastAPI or Flask), packaged as its own Dockerfile (plain Debian/Alpine base image, not the hardened n8n image). (`podcast-crop-detector/Dockerfile`, `requirements.txt`, `src/server.py` - Flask chosen over FastAPI for fewer transitive deps)
- [x] 1.2 Bundle the CPU DNN face-detection model (`res10_300x300_ssd` or equivalent small, permissively-licensed model) into the image. (`podcast-crop-detector/models/deploy.prototxt` + `res10_300x300_ssd_iter_140000.caffemodel`, downloaded from the OpenCV project's own repos)
- [x] 1.3 Implement `POST /crop-position`: accepts a video file path (resolved against the shared `/home/node/.n8n-files` mount) plus a start/end time range, samples a handful of frames across that range, detects faces per frame, scores each detected face by mouth-region motion across samples, and returns `{found: true, xOffset: <px>}` for the highest-scoring face or `{found: false}` when no face is detectable. (`podcast-crop-detector/src/detector.py` - track-and-score by mouth-ROI frame-diff; `src/server.py` wires the HTTP contract)
- [x] 1.4 Add a lightweight health-check endpoint (e.g. `GET /health`) for compose/orchestration readiness checks. (`podcast-crop-detector/src/server.py`)
- [x] 1.5 Unit-test the frame-sampling and motion-scoring logic against a few short local clips (including a case with 0, 1, and 2+ faces) before wiring it into production. (`podcast-crop-detector/tests/test_detector.py`, run for real inside the built Docker image on the VPS against 3 real clips from the already-processed test episode - not simulated: 1-face clip → `faceCount:1, confidence:0.986`; 2-face clip → `faceCount:2, confidence:0.952`; faceless-torso clip → `found:false`. NOTE: fixtures are already-cropped 1080x1920 outputs of the OLD center crop, not the 7.5GB landscape original (impractical to keep as a repo fixture) - this validates detection/motion-scoring logic runs correctly on real footage across the 0/1/2-face spread; end-to-end validation of the actual off-center xOffset math against the real landscape source happens in tasks 4.1-4.3)

## 2. Deployment

- [x] 2.1 Add a `docker-compose.yml` for the sidecar on the VPS (mirroring the existing `clip-studio`/`n8n` compose pattern), mounting the same named volume the n8n container uses for `/home/node/.n8n-files` as read-only, and joining the shared `n8n_default` network. (`podcast-crop-detector/deploy/docker-compose.yml`, modeled directly on `cookie-refresher/deploy/docker-compose.yml`'s bind-mount of `/root/n8n/n8n_files`)
- [x] 2.2 Deploy and confirm the health-check endpoint responds before touching the n8n workflow. (Deployed via `docker compose -f deploy/docker-compose.yml up -d --build` on the VPS; confirmed `podcast-crop-detector-1` reachable by name from `n8n-n8n-1` on `n8n_default` - `wget -qO- http://podcast-crop-detector-1:4610/health` → `{"status":"ok"}`)

## 3. n8n workflow integration (`Zfwhv4pppJf3NhkR`)

- [x] 3.1 Add a new HTTP Request node (e.g. "Detectar Rosto Ativo Podcast") right before "FFmpeg Cortar 9:16 Podcast", calling the sidecar's `/crop-position` with the clip's ~~real `ASTART`/`AEND` (post-silence-snap)~~ and the source video path, with a bounded timeout (~15s) and `onError: continueRegularOutput` so a failure never aborts the clip. **Deviation from the original wording (surfaced, not silently narrowed):** `ASTART`/`AEND` are computed INSIDE the "FFmpeg Cortar 9:16 Podcast" bash script itself (silence-snap depends on the raw `clipStart`/`clipEnd` the AI picked) - they don't exist yet at the point this new node runs, one step earlier. Used `clipStart`/`clipEnd` (the AI's raw pre-snap picks) instead: the silence-snap only shifts by a few seconds, which doesn't change who's speaking over a 30-180s clip, so sampling that slightly wider raw range is an accurate-enough proxy without restructuring the snap logic into two network round-trips.
- [x] 3.2 Update "FFmpeg Cortar 9:16 Podcast"'s crop expression: use the returned `xOffset` when the previous node found a face; otherwise (`found:false`, timeout, or error) fall back to today's `(iw-1080)/2` fixed-center formula - implemented as a code-level fallback, never a hard dependency on the sidecar's response. Also fixed a coordinate-space bug found while wiring this up: the sidecar detects faces on the ORIGINAL video, but the crop filter runs AFTER `scale=-2:1920` in the ffmpeg command - `detector.py`'s `find_crop_position` now takes an `outputHeight` param and scales the returned `xOffset` into the post-scale coordinate space (mirroring ffmpeg's own `-2` even-width rounding) before the caller ever sees it, so the crop lands on the same pixel the sidecar actually detected.
- [x] 3.3 Validate the updated Code/Execute Command node syntax locally (`new Function()` / `sh -n`) before applying to production, matching this project's established validation practice. Validated both branches (`CROPX` populated and `CROPX` empty/fallback) with `sh -n` against the real n8n expressions substituted with representative literal values - both passed.
- [x] 3.4 Apply via MCP (`update_workflow`) and `publish_workflow`, confirming byte-for-byte that the Shorts/Palavra Completa workflows remain untouched. Applied atomically (addNode + setNodeSettings + 2 connection ops + updateNodeParameters), re-fetched and confirmed the FFmpeg node's command matches the intended string byte-for-byte, confirmed `search_workflows` shows "Blocos" (`ID4wisnN4Tqpt2zh`, updatedAt 2026-09-02) and "Palavra Completa" (`knWdWza8AxNht0oJ`, updatedAt 2026-09-08) both untouched today. Published - `activeVersionId: 1456eea3-19e7-4ef7-82a9-6db33b99fc50`.

## 4. Validation

- [~] 4.1 Re-run the already-processed test episode (or a new short podcast clip) through the updated pipeline. **In progress:** the archived source video (`cmtthbjcu00040img1bah6t2h_PodCast.mov`) was moved back from `Videos-Cortes/Podcast/Videos` to the queue folder `Videos-Cortes/Podcast` (via 2 temporary HTTP nodes, user-authorized, removed immediately after use) and the pipeline was triggered for real - execution `1663`, started 2026-09-09T15:04:59Z, `status:"running"`. Whisper.cpp + 2 AI passes + N clip cuts (each now also calling the face-detection sidecar) takes ~2-3h+ based on this project's historical runs of this same episode (see execution #1556, ~3h21min). Not complete as of this session ending - needs a follow-up check.
- [ ] 4.2 Extract frames from the resulting clips (same technique used to diagnose this issue) and confirm the crop keeps a face in frame for clips where the old fixed-center crop missed it. **Blocked on 4.1 finishing.**
- [ ] 4.3 Deliberately exercise the fallback path (e.g. stop the sidecar container) and confirm a clip still gets cut successfully with the old fixed-center crop, not a failure.
- [ ] 4.4 Document the outcome in CLAUDE.md, including any tuning needed (timeout, sampling count, motion-score threshold) discovered during real validation. **Blocked on 4.1/4.2/4.3.**
