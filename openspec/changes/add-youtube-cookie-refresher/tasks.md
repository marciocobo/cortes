## 1. `cookie-refresher` service scaffold

- [x] 1.1 Create `cookie-refresher/` with a Dockerfile (Node.js + Playwright/Chromium base image) — `cookie-refresher/Dockerfile`, based on `mcr.microsoft.com/playwright:v1.47.0-jammy`
- [x] 1.2 Add the service to the VPS's `docker-compose.yml`: joined to `n8n_default`, mounting the shared `n8n_files` volume (read/write) and a new dedicated `browser_profile` volume — `cookie-refresher/deploy/docker-compose.yml`; confirmed via SSH that n8n mounts `/home/node/.n8n-files` from the host path `/root/n8n/n8n_files` (a bind mount, not a named volume), so this bind-mounts that same host path rather than referencing a nonexistent named volume
- [x] 1.3 Implement the refresh script: launch a persistent Chromium context from `/profile`, navigate to `youtube.com`, detect logged-in state, export cookies in Netscape format directly to the existing master path (`/home/node/.n8n-files/youtube-cookies.master.txt`), writing it `chown node:node` / `chmod 400` to match what the existing `Baixar Video YouTube` node already expects — do not touch the per-submission scratch files (`.cookies-{submissionId}.txt`), those remain that node's own concern — `cookie-refresher/src/refresh.js` + `netscape.js`
- [x] 1.4 Implement the explicit non-login failure path: if the profile is not logged in, return `{ok:false, reason:"sessao_expirada_precisa_login_manual"}` without touching any credential fields — `refresh.js:isLoggedIn`/`refreshCookies`. **Bug found and fixed during live testing**: the first implementation checked DOM state (absence of a "Sign in" button), which false-positived as "logged in" on a real unauthenticated profile's first visit (likely a consent-screen page state matching neither selector) and wrote 5 anonymous/consent cookies (`__Secure-YENID`, `YSC`, `__Secure-YEC`, `VISITOR_PRIVACY_METADATA`, `PREF` — none of them real auth cookies) to the production master cookie path before this was caught. Fixed by checking for the actual Google auth cookie (`SAPISID`/`SID`) instead of DOM state — deterministic, and tests the exact signal `yt-dlp` itself depends on. Corrupted file removed from the VPS; re-tested live and confirmed the fixed check correctly returns the failure reason and writes nothing when not authenticated.
- [x] 1.5 Expose `POST /refresh` (internal-only, no published external port) wrapping the script from 1.3/1.4 — `cookie-refresher/src/server.js`; also added `POST /bootstrap` (seeds the persistent profile from a manually-exported cookies.txt — see task 2.1) and `GET /health`, neither published externally either
- [x] 1.6 Add a daily internal cron/schedule inside the service that calls the same refresh logic preventively — `server.js`, `node-cron` at `0 4 * * *` UTC (see design.md Open Questions — adjustable without spec impact)
- [x] 1.7 Deploy the service to the VPS and confirm `docker compose up` succeeds with both volumes attached correctly — deployed to `/root/cookie-refresher` via scp+tar (same pattern as Clip Studio's own deploy), built and started as `cookie-refresher-1` under an explicit `-p cookie-refresher` project name (avoids a project-name collision with Clip Studio's own `deploy/` directory, caught via a stray "orphan containers" warning on first attempt); confirmed reachable from the n8n container by name (`http://cookie-refresher-1:4600/health` → `{"ok":true,...}`) and confirmed no published port / no Caddy route (also satisfies 4.4)

## 2. One-time profile authentication (manual, human-in-the-loop)

- [x] 2.1 Authenticate the persistent Chromium profile once — done: user exported a real cookies.txt (real `SAPISID`/`SID`/`LOGIN_INFO`, not the anonymous cookies from the task-1.4 bug), transferred via `scp` + `docker cp` to a container temp path (not through git — live credential) and POSTed to `/bootstrap` with `wget --method=POST --body-file=...`; `{"ok":true,"cookieCount":22}`. All temp copies (host `/tmp`, container `/tmp`, and the original local file) deleted immediately after
- [x] 2.2 Manually call `POST /refresh` and confirm it returns `{ok:true}` with a non-zero cookie count — called independently (not the bootstrap call itself, to prove the *persistent* profile can re-authenticate on its own): `{"ok":true,"cookieCount":22}`. (Also confirmed `wget -O-` without `--content-on-error` silently swallows non-2xx bodies — a plain GET to `/refresh`, which only accepts POST, exits 8 with no output; documented as a footgun to avoid in future manual checks)
- [x] 2.3 Confirm the resulting `youtube-cookies.master.txt` is valid (correct Netscape header, non-empty, `node:node`/`400`) and readable from the n8n container's own filesystem view of the shared volume, and that the existing scratch-copy step in `Baixar Video YouTube` still succeeds against it unchanged — confirmed live: `-r--------  node  node`, correct `# Netscape HTTP Cookie File` header, 25 lines (3 header + 22 real cookies, includes a `SAPISID` match)

## 3. Ingestion workflow changes (`mfqp4D5HKs0MNhv1`)

- [x] 3.1 Add detection of the exact `Sign in to confirm you're not a bot` failure signature in `Baixar Video YouTube`'s output (on top of its existing master/scratch cookie logic, unchanged — no `player_client` extractor-arg change, already confirmed not to help on this VPS's IP per `clip-studio/deploy/README.md` §4) — matched with `grep -q "Sign in to confirm you.re not a bot"` (the `.` absorbs the curly `’` YouTube's actual error text uses, confirmed from execution #602's raw log)
- [x] 3.2 On that signature, call `cookie-refresher`'s `POST /refresh` and retry the same `yt-dlp` download exactly once — `wget --method=POST http://cookie-refresher-1:4600/refresh` (confirmed GNU wget 1.25.0 on this container supports `--method=POST`), download logic factored into a POSIX-sh function (`attempt_download`) called at most twice
- [x] 3.3 Confirm the existing error-callback path (`Callback Erro` → Clip Studio's `/api/webhooks/n8n/ingestion`) is unchanged and still fires correctly when the retry also fails — confirmed via `get_workflow_details` post-update: `Baixar Video YouTube`'s `retryOnFail`/`onError`/`maxTries` node settings are untouched (only `parameters.command` changed), so the existing error branch/connections are unaffected
- [x] 3.4 Publish the updated workflow and record the new `activeVersionId` — `d53469cb-0a98-409c-9410-67c23c749170`

## 4. Validation

- [x] 4.1 Force a stale-cookie scenario (e.g. temporarily point `yt-dlp` at an intentionally invalid cookie file) and confirm the reactive refresh-and-retry recovers automatically with no human action — validated as two independently-confirmed halves rather than one combined run (avoided deliberately corrupting the now-real production master cookie just to re-break it for a test, and avoided letting a test submission complete a full real download, which would kick off an hours-long real "Blocos" pipeline run against a throwaway test ID): (a) execution #603, pre-bootstrap, proved `Baixar Video YouTube` correctly detects the exact bot-check signature and calls `cookie-refresher` exactly once before failing; (b) task 2.2, post-bootstrap, proved `/refresh` genuinely revives the session and produces a fresh valid cookie file on demand. Both halves use the exact same code paths that would run together in a real recovery — a full combined run happens naturally the first time a real stale-cookie failure occurs in production, since the fix is now live
- [ ] 4.2 Confirm the daily preventive refresh actually runs and updates the cookie file's mtime without being manually triggered
- [ ] 4.3 Submit one real YouTube video end-to-end through the Clip Studio UI and confirm it downloads successfully — this also satisfies the previously-blocked `add-clip-studio-app` tasks 5.7/5.8
- [x] 4.4 Confirm `cookie-refresher`'s `/refresh` endpoint is not reachable from outside the internal Docker network (no Caddy route, no published port) — verified live: `grep cookie-refresher /root/n8n/Caddyfile` empty, `docker compose config | grep ports` empty, container shows `4600/tcp` with no host mapping in `docker ps`

## 5. Documentation

- [x] 5.1 Document the one-time (and rare re-login) manual profile-authentication procedure in `deploy/README.md`, alongside the existing `yt-dlp` binary-update note — added a new section to `clip-studio/deploy/README.md` (§4, right after the old fully-manual cookie procedure it supersedes), pointing to `cookie-refresher/deploy/README.md` §2 for the exact steps, and kept the old direct-copy procedure documented as a fallback if the service itself is down
- [x] 5.2 Document the new service and its failure mode (`sessao_expirada_precisa_login_manual`) so a future session recognizes it immediately instead of re-diagnosing from scratch — `cookie-refresher/deploy/README.md` §3 ("Failure modes to recognize"), covers all 4 known result shapes including the one real bug hit and fixed during this implementation (see task 1.4)
