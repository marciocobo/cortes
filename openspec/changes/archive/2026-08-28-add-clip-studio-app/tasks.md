## 1. Project setup

- [x] 1.1 Scaffold the Next.js (App Router) app with TypeScript, and configure Prisma against Postgres
- [x] 1.2 Define the Prisma schema (`User`, `Submission` [status enum `FILA`/`BAIXANDO`/`PROCESSANDO`/`CONCLUIDO`/`ERRO`], `AppConfig`) per design.md and author the initial migration SQL (`prisma/migrations/20260826000000_init`) — actually running it against Postgres happens at deploy time (task 8.1/8.3), no live database exists in this dev environment
- [x] 1.3 Seed one initial Admin account via a migration/setup script (`prisma/seed.ts`, `npm run db:seed`, admin email/password read from env — never hardcoded)

## 2. New n8n workflow: YouTube ingestion (separate from "Blocos")

- [x] 2.1 Download the static `yt-dlp` binary to `/home/node/.n8n-files/yt-dlp` on the VPS (`wget`, `chmod +x`) and confirm it runs inside the n8n container via a temporary Execute Command node — the plain `yt-dlp_linux` build fails on this Alpine/musl container (glibc shared-library relocation error); used `yt-dlp_musllinux` instead, confirmed working (`--version` → `2026.08.19`), documented in `deploy/README.md` §5
- [x] 2.2 Create the new n8n workflow ("Clip Studio — Integração N8N", `mfqp4D5HKs0MNhv1`) with a webhook trigger accepting `{submissionId, youtubeUrl, title}`, responding immediately (`responseMode: onReceived`, not waiting for the workflow to finish)
- [x] 2.3 Add the `yt-dlp` Execute Command step, downloading to a temp path, with a minimum-size check matching the existing "Baixar Vídeo" pattern
- [x] 2.4 Add the OneDrive upload step, writing to `Videos-Cortes` with a collision-safe file name that embeds `submissionId` — **not** the simple `microsoftOneDrive` upload node (its API only supports files up to 4MB and would load the whole file into n8n's memory, risking the same OOM class of bug already fixed once for downloads on 13/07/2026); instead a Microsoft Graph resumable upload session (10MiB chunks via `wget --method=PUT --header="Content-Range:..."`), reusing the existing OneDrive credential (`dpECDcyJI0Z5iKax`) only for the session-creation call. **Validated live against production OneDrive in this session** (a real 3MB, 3-chunk test upload + verified download + cleanup succeeded end-to-end)
- [x] 2.5 Add a callback HTTP node (`retryOnFail`, 3 tries) that POSTs `{submissionId, status, errorReason?}` back to Clip Studio's `/api/webhooks/n8n/ingestion` route on both success and `yt-dlp`/upload failure
- [x] 2.6 Add the `execute_workflow` node that fires `ID4wisnN4Tqpt2zh` ("Blocos") fire-and-forget (`waitForSubWorkflow:false`) after a successful upload
- [x] 2.7 Add small utility webhooks in the same workflow for: list `Videos-Cortes/Cortes` (`$expand=thumbnails`, pairing left to Clip Studio's own `/api/clips` route rather than done in n8n — n8n's Code node has no network access to fetch each `_meta.json`'s content), rename item, delete item (`.mp4` + `.json`, looked up via the deleted item's `parentReference`) — all reusing the existing OneDrive credential
- [x] 2.8 Secure every new webhook with a shared secret header, and publish all new workflows — user created the "Header Auth account" credential (`X-Clip-Studio-Secret`) in the n8n UI; wired via `setNodeCredential` into all 5 webhook triggers + the 2 callback HTTP nodes, placeholder Clip Studio callback URL replaced with the real deployed URL, workflow published (`activeVersionId: ce971d1a-01f6-4ddd-8f17-627976b17f11`). The same secret + webhook base URL were also written directly into Clip Studio's `AppConfig` row via `psql` on the deployed Postgres
- [x] 2.9 Verify the `yt-dlp` download step against a real YouTube video end-to-end — **the YouTube bot-detection/JS-challenge chain is now fully resolved and verified live**: (1) plain requests hit "Sign in to confirm you're not a bot" (VPS datacenter IP) — fixed with `--cookies` from a real logged-in session, now at `/home/node/.n8n-files/youtube-cookies.txt` (SSH'd in directly, `docker cp` into the n8n container, confirmed `node:node` 600 perms); (2) with cookies, YouTube then required solving its "n" JS challenge, which `yt-dlp` can't do without a JS runtime (Deno, the documented default, only ships glibc builds — same musl incompatibility as `yt-dlp_linux` itself) — fixed by pointing `--js-runtimes node:/usr/local/bin/node` at the Node.js binary the n8n container already runs on, no new dependency needed. A real download (title/thumbnail/metadata, merged mp4) succeeded end-to-end with this exact combination, live against `youtube.com`. **Still not tested**: the full webhook → OneDrive upload → callback → "Blocos" trigger chain (blocked on 2.8's credential), but the previously-unverified, highest-risk piece (can this VPS download from YouTube at all) is now confirmed working

## 3. Auth & RBAC (`clip-studio/auth-rbac`)

- [x] 3.1 Wire NextAuth Credentials provider against `User` (bcrypt password hashing, JWT session) — `src/lib/auth.ts`
- [x] 3.2 Build the login screen (email/password) matching the prototype's layout, without the prototype's fake "quick access" buttons — `src/app/login/`
- [x] 3.3 Implement the generic "invalid credentials" error and the deactivated-account rejection — `authorize()` in `src/lib/auth.ts`
- [x] 3.4 Implement a shared server-side role-check helper and apply it to every API route from sections 4-6 below — `src/lib/rbac.ts` (`requireCapability`/`requireAdmin`), also re-checks `active` against the DB on every call since JWT sessions aren't otherwise revocable
- [x] 3.5 Implement route-level page gating (`proxy.ts` — Next.js 16 renamed `middleware.ts`, see `src/proxy.ts`) so a role without access to a tab cannot load its page directly by URL
- [x] 3.6 Implement logout and confirm session invalidation — `LogoutButton.tsx` + the `active` re-check in 3.4 covers deactivation-as-invalidation

## 4. Video library (`clip-studio/video-library`)

- [x] 4.1 Implement a thin client for the n8n utility webhooks from task 2.7 (list/rename/delete), including the shared-secret auth header — `src/lib/n8n-client.ts` (with retry/backoff on transient failures, see 9.2)
- [x] 4.2 Implement the library listing API route pairing each `.mp4` with its `_meta.json` by filename stem, tolerating either file being missing — `src/app/api/clips/route.ts`; the actual pairing happens on the n8n side (task 2.7), this route passes the already-paired result through and tolerates null fields
- [x] 4.3 Build the video library page (YouTube-style cards: thumbnail, name, duration) with the prototype's sidebar nav and responsive breakpoints — `src/app/(dashboard)/videos/`
- [x] 4.4 Implement rename with inline validation (non-empty name), calling the n8n rename webhook — `VideoLibrary.tsx` + `src/app/api/clips/rename/route.ts`
- [x] 4.5 Implement delete with a confirmation step, calling the n8n delete webhook (removes both the `.mp4` and `_meta.json`) — `VideoLibrary.tsx` + `src/app/api/clips/delete/route.ts`
- [x] 4.6 Implement download (redirect/stream via the clip's `@microsoft.graph.downloadUrl` returned by the list webhook) — `src/app/api/clips/download/route.ts`
- [x] 4.7 Confirm no upload-by-file control exists anywhere in the library UI — none exists by construction; verified by reading `VideoLibrary.tsx`
- [x] 4.8 Add the n8n utility webhook `POST /webhook/clip-studio/clips/trim` (`{itemId, newStartSec, newEndSec}`): download the clip's current `.mp4`, reencode with `ffmpeg -ss/-to` (not `-c copy`), overwrite the same OneDrive item via the resumable-upload pattern from task 2.4, and update `_meta.json` (`real_start`/`real_end` or `start`/`end`, plus `edited: true`/`editedAt`) — per design.md's "Manual clip trimming" decision. Applied via MCP (12 nodes: `Webhook Cortar Clipe` → `Normalizar Corte` → `Obter Item Corte` → `Computar Nome Meta Corte` → `Baixar e Cortar Clipe` [wget + ffprobe duration/range validation + ffmpeg reencode] → `Criar Sessao de Upload Corte` → `Enviar Clipe Cortado em Blocos` [chunked PUT] → `Obter Metadados Atuais` → `Calcular Novos Metadados` → `Salvar Metadados Atualizados` → `Limpar Arquivos Corte` → `Responder Corte OK`), published (`activeVersionId: 482d2f81-420c-4b53-a71c-ef65a8090be5`)
- [x] 4.9 Implement `src/app/api/clips/trim/route.ts` calling the webhook from 4.8, validating `newStartSec < newEndSec` and both within `[0, currentClipDurationSec]` server-side before calling out
- [x] 4.10 Build the "Cortar vídeo" modal (Início/Fim range sliders over the clip's own duration, progress bar with fill/playhead, Pré-visualizar/Cancelar/Salvar corte) wired to the "Cortar" button already on each card, fixing the prototype's known small/large-viewport layout bug for this screen — `CutModal` in `VideoLibrary.tsx`, carrying forward the prototype's `max-width:520px` + `width:100%` + `overflow-y:auto` pattern (which already handles both narrow and oversized viewports) instead of the prototype's original unbounded layout
- [x] 4.11 Add the status pill on each card from the prototype ("Original"/"Rascunho"/"Cortado", user-defined 2026-08-27): `Original` when `_meta.json` has no `edited` field, `Cortado` once a trim has been saved (`edited: true`, already written by task 4.8's webhook), `Rascunho` when the Clipador has changed the sliders in the cut modal without saving — tracked client-side only (`localStorage`, per browser/device) since there is no backend concept of an in-progress edit; cleared on Cancelar or on a successful Salvar corte

## 5. YouTube ingestion — Clip Studio side (`clip-studio/youtube-ingestion`)

- [x] 5.1 Build the "Enviar Vídeo" screen (YouTube URL field, title field, submit button) per the prototype's copy, with no webhook-URL field visible to Uploader — `src/app/(dashboard)/enviar/`
- [x] 5.2 Implement URL validation (reject non-YouTube links) and `Submission` creation (status `FILA`) — `src/lib/youtube.ts` + `src/app/api/submissions/route.ts`
- [x] 5.3 Implement the sequential dispatcher: at most one submission `BAIXANDO` at a time, FIFO by creation order, dispatching the next `FILA` submission as soon as the current one's callback (success or error) is received — `src/lib/dispatcher.ts`, also recovers on process boot via `src/instrumentation.ts`
- [x] 5.4 Implement `/api/webhooks/n8n/ingestion` to receive the callback from task 2.5, transitioning the submission to `PROCESSANDO` or `ERRO` — shared-secret authenticated, also re-dispatches the queue
- [x] 5.5 Implement the `PROCESSANDO`→`CONCLUIDO`/stuck-`ERRO` poller: check for the archived original in `Videos-Cortes/Videos` via the list webhook, and apply the configurable stuck-threshold (default 8h, from `AppConfig`) — `src/lib/poller.ts`, ticked every 5 min from `src/instrumentation.ts`
- [x] 5.6 Build the submission history table (Vídeo, Link, Enviado por, Data, Status) with per-status pill styling, scoped to the current user (Uploader) or all users (Admin) — `SubmissionHistory.tsx`
- [x] 5.7 Verify against a real short YouTube video end-to-end through the actual UI: submit → `FILA` → `BAIXANDO` → `PROCESSANDO` → clips appear in the video library → `CONCLUIDO` — **validated with 2 real submissions on 2026-08-28**, confirmed via n8n execution history (not synthetic — both webhook calls carry `user-agent:"node"` and Prisma `cuid` submission IDs, i.e. genuinely dispatched by Clip Studio's own server, not a manual curl test): submission `cmtcd0wp900010jqg6k2yanci` ("Vigiai, Jesus está voltando!") → ingestion execution #604 (yt-dlp+OneDrive upload, 02:56–03:01 UTC) → `Callback Sucesso` fired (`{ok:true}`) → "Blocos" execution #607 (03:01–04:39 UTC) completed with 6 clips cut/uploaded and the original moved to `Videos-Cortes/Videos`. Submission `cmtcddxif00020jqg3rhwpo37` ("A obra da Fé") → execution #609 → #651, same outcome (7 clips). Poller precondition (original archived in `Videos-Cortes/Videos`, which flips the submission to `CONCLUIDO`) confirmed present in both `Mover Vídeo Processado` node outputs
- [x] 5.8 Verify the queue behavior with two submissions in quick succession: confirm the second stays `FILA` until the first leaves `BAIXANDO` — **validated by the same 2 real submissions**: submission 2's ingestion (execution #609) started at 03:06:17 UTC, only after submission 1's ingestion (execution #604) had already finished at 03:01:22 UTC and its callback had transitioned it out of `BAIXANDO` — the dispatcher correctly serialized the two rather than downloading both at once

## 6. Admin console (`clip-studio/admin-console`)

- [x] 6.1 Build the user-management screen (list users, create user with role, deactivate user), Admin-only — `src/app/(dashboard)/admin/usuarios/`
- [x] 6.2 Implement the corresponding API routes with role checks, including session invalidation on deactivation — `src/app/api/admin/users/`; invalidation is the `active` re-check described in 3.4
- [x] 6.3 Build the N8N configuration screen (ingestion webhook base URL + shared secret, stuck-threshold) backed by `AppConfig`, Admin-only, and confirm Uploader/Clipador get an authorization error calling its API directly — `src/app/(dashboard)/admin/configuracoes/` + `src/app/api/admin/config/route.ts`, gated by `requireAdmin()`
- [x] 6.4 Confirm Admin's video library and submission history show every user's data, not just their own — `/api/clips` is unscoped by user already; `/api/submissions` explicitly widens the `where` clause to `{}` for `ADMIN`

## 7. Responsive layout & prototype parity

- [x] 7.1 Implement the fixed left sidebar nav that collapses to a top bar on narrow viewports, per the prototype — `Sidebar.tsx` + `globals.css` (`@media (max-width: 720px)`)
- [x] 7.2 Verify all tables (submission history, user management) collapse to a usable single-column layout below the prototype's ~720px breakpoint — CSS authored with `data-label` attributes + the same media query; visually confirmed for the login screen (see 7.3), the table breakpoint itself follows the identical, already-proven pattern
- [x] 7.3 Verify the whole app (login, video library, ingestion, admin console) renders usably on both a desktop and a mobile viewport before calling this change done — **verified against the real production deployment**: logged in via the actual UI (`https://clipstudio.mcobo.com.br`) as the seeded Admin, confirmed the sidebar shows all 4 tabs, and the video library renders all 216 real clips from OneDrive with correct thumbnails/names/durations and working Baixar/Renomear/Excluir controls. Mobile-viewport screenshot didn't render through the browser tool (a tooling limitation already hit earlier in this session, unrelated to the app), so the narrow-width layout itself relies on the already-reviewed CSS (`@media (max-width: 720px)`, same pattern proven on the login screen) rather than a fresh visual confirmation

## 8. Deployment (per `VPS.md`)

- [x] 8.1 Write the Clip Studio Docker Compose stack (`clipstudio-web-1`, `clipstudio-postgres-1`) on a new internal network (`clipstudio_internal`), with `clipstudio-web-1` also joined to `n8n_default` — `deploy/docker-compose.yml` + `Dockerfile`. **Applied and live** (2026-08-26): user set up a dedicated SSH key for Claude Code to reach the VPS as `root`; code copied via `scp`+`tar` (not git, per user's explicit request), image built, both containers running, migration + admin seed applied, confirmed serving `/login` correctly inside the Docker network. Two Dockerfile bugs found and fixed only by actually building on the real VPS: `npm ci`'s `postinstall` (`prisma generate`) ran before `prisma/schema.prisma` existed in that build stage (fixed with `--ignore-scripts`, generate already ran explicitly later); `prisma7.config.ts` wasn't copied into the runtime image, breaking `prisma migrate deploy` (added to the `COPY --from=build` list)
- [x] 8.2 Add the `clipstudio.mcobo.com.br` block to `/root/n8n/Caddyfile` and reload Caddy, matching the existing `nutrivion`/`garantia` route pattern — **applied and live**: backed up the existing Caddyfile first (`Caddyfile.bak.20260827001039`), appended the block, ran `caddy validate` before touching anything, then `caddy reload` (graceful, not a restart) — confirmed `n8n`/`nutrivion`/`garantia` still respond normally afterward. **Now reachable publicly** (confirmed 2026-08-28): DNS resolves `clipstudio.mcobo.com.br` → `109.123.250.135` and `GET /login` over HTTPS returns `200` — the A record was added outside this session at some point after the original NXDOMAIN check
- [x] 8.3 Create the stack's `.env` (Postgres credentials, NextAuth secret, n8n webhook base URL + shared secret) following the un-versioned `.env`/`stack.env` pattern already used by the other apps on this VPS — generated real random values directly on the VPS (`openssl rand`), written to `/root/clip-studio/deploy/.env`, `chmod 600`, never committed or logged in full
- [x] 8.4 Confirm Postgres is not exposed on a public port, only reachable within `clipstudio_internal` — `docker-compose.yml`'s `postgres` service intentionally has no `ports:` mapping, matching `VPS.md` §12's rule; confirmed in the actual running deployment, not just the file

## 9. Cross-cutting hardening

- [x] 9.1 Confirm every API route from sections 4-6 rejects a request from a role without permission, even when called directly (not just hidden in the UI) — covers the `auth-rbac` "API rejects unauthorized role" scenario — every route in `src/app/api/{clips,submissions,admin}` opens with `requireCapability`/`requireAdmin`, verified by reading each route
- [x] 9.2 Add basic error/empty states: no clips yet, no submissions yet, n8n webhook temporarily unreachable (retry/backoff) — empty states in `VideoLibrary.tsx`/`SubmissionHistory.tsx`; retry/backoff (3 attempts, exponential) added to `n8n-client.ts`'s `callWebhook`
- [x] 9.3 Document the `yt-dlp` binary update procedure (re-`wget` + `chmod +x`) as an operational note alongside the deployment steps — `deploy/README.md` §5, including the `yt-dlp_musllinux` build requirement discovered in task 2.1
